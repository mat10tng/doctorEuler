#include <vector>
using namespace std;
template<class T>
class large_vector {
	vector<vector<T>> the_vector;
	int MAX_LENGTH;
	large_vector(unsigned long long int a){
		the_vector.push_back(vector<T>);
		MAX_LENGTH = the_vector[0].maxsize();
	}
};
